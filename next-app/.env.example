# PostgreSQL 接続先。docker compose up -d db で起動するローカル DB の例:
DATABASE_URL=postgresql://dev:dev@localhost:5432/tokyo_subsidy

# 問い合わせ・削除依頼の窓口メール。/contact ページに表示される。
# 本番では実アドレス必須 (自治体からの連絡を受ける窓口)。Gmail+alias 推奨。
CONTACT_EMAIL=contact@example.com
